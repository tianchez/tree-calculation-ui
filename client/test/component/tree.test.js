import React from 'react';
import { shallow } from 'enzyme';
import TreeComponent from '../../src/component/tree/treeComponent';
import Constants from '../../src/Constants'

describe('TreeComponent tests', () => {

    // TODO: Add test cases
    it('TreeComponent is loaded', () => {
        expect(0).toBe(0);
      });
  });