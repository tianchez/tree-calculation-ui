import React from 'react';
import { shallow } from 'enzyme';
import UserService from '../../src/service/userService';
import Constants from '../../src/Constants'

describe('UserService tests', () => {

    // TODO: Add test cases
    it('UserService is invoked', () => {
        expect(0).toBe(0);
      });
  });