import React from 'react';
import { shallow } from 'enzyme';
import TreeService from '../../src/service/treeService';
import Constants from '../../src/Constants'

describe('TreeService tests', () => {

    // TODO: Add test cases
    it('TreeService is invoked', () => {
        expect(0).toBe(0);
      });
  });